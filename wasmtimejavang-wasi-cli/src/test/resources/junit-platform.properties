# Safety net for hangs: fail a test after 60s instead of riding out the CI
# job's overall timeout with no diagnostics. 60s is generous headroom over
# this suite's longest explicit wait (10s, in the wasip2 socket tests).
junit.jupiter.execution.timeout.default = 60s
junit.jupiter.execution.timeout.thread.dump.enabled = true
